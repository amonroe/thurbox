# The built-in "hooks" extension.
#
# Wires each coding agent's lifecycle hooks to `thurbox-cli session signal` so
# every session reports its state (working / blocked / done) back to thurbox.
# Unlike user extensions this one ships embedded in the binary and is
# auto-activated on first run. Opt out with:
#
#     thurbox-cli extension deactivate hooks
#
# Identity is the injected $THURBOX_SESSION env var (no ids passed by hand), and
# every hook command is `… || true` so a missing thurbox-cli or a hook firing
# outside a thurbox session can never break the agent.

name = "hooks"
description = "Report each agent's lifecycle state (working/blocked/done) to thurbox via agent hooks."
config_version = 1
version = "1.9.0"  # v1.9: claude/antigravity/grok match the *message* of a Notification rather than the whole payload — a checkout path carrying "permission" signalled a permanent `blocked` on the idle notification that ends every turn; v1.8: grok (global ~/.grok/hooks drop) + kimi (TOML merge into ~/.kimi-code/config.toml) status hooks; v1.7: omp (Oh My Pi) status extension (ask/ask_user_question → blocked); v1.6: vibe hooks rewritten to vibe 2.21.0's real schema (pre_tool/post_agent); v1.5: pi (pi.dev) status extension
# Default home; the auto-activation path overrides this with *this build's*
# resolved config dir (`~/.config/thurbox` or `~/.config/thurbox-dev`) so dev and
# release installs stay isolated. See `session_ops::builtin_hooks::hooks_home`.
home = "~/.config/thurbox/hooks"

# claude reads hooks from a settings file we pass via --settings (merged with the
# user's own settings, never clobbering them). The file lives under the home dir.
#
# Its `Notification` hook matches permission/approval against the payload's
# `message` and nothing else. Globbing the whole payload looked equivalent and is
# not: `cwd` and `transcript_path` are the operator's own directory names, so a
# checkout under a path with "permission" in it signalled `blocked` on the idle
# notification that fires a minute after every turn ends — and that notification
# is the last hook such a session ever fires, so the state stood for good. The
# block edge stays a text match either way (`blocked_is_heuristic`); what changed
# is which text.
[[files]]
path = "claude.json"

# Inject the --settings flag into the existing built-in `claude` agent (reversible).
[[agent_patches]]
name = "claude"
append_args = ["--settings", "{home}/claude.json"]

# aider has no lifecycle hook system — only a "waiting for input" callback, which
# maps to blocked. (working/done aren't expressible; the dot stays accurate for
# the block edge.)
[[agent_patches]]
name = "aider"
append_args = [
  "--notifications",
  "--notifications-command",
  "thurbox-cli session signal --state blocked",
]

# opencode loads plugins from its own config dir; drop one in (only when opencode
# is actually installed, guarded by requires_dir).
[[external_files]]
path = "~/.config/opencode/plugin/thurbox-status.js"
source = "opencode-status.js"
requires_dir = "~/.config/opencode"

# codex now has a claude-shaped hooks.json (SessionStart/UserPromptSubmit/
# PreToolUse/Stop) loaded from ~/.codex/hooks.json — strictly richer than the
# old `notify` (which only fired agent-turn-complete → done). We JSON-merge our
# entries into that file (reversibly; uninstall prunes exactly ours by the
# `session signal` marker, leaving the user's hooks intact), guarded by
# requires_dir so it's a no-op when codex isn't installed. Events: SessionStart
# → idle, UserPromptSubmit/PreToolUse → working, Stop → done. No `blocked` —
# codex's top-level hooks have no permission/approval event (that lives only in
# the legacy `notify`).
#
# EXPERIMENTAL: codex's hooks.json is newer than its `notify`; the event names
# here are assumed identical to claude's. If they differ on the live `codex`,
# edit codex-hooks.json (no code change). Replaces the old `-c notify=…` patch
# (avoiding a double-`done` signal); the trade is a reversible write into a
# separate ~/.codex/hooks.json — never the user's config.toml.
[[config_merges]]
path = "~/.codex/hooks.json"
source = "codex-hooks.json"
requires_dir = "~/.codex"

# vibe (Mistral Vibe) loads hooks from ~/.vibe/hooks.toml. It's TOML, so we
# can't JSON-merge it — we drop a managed file in (only when vibe is installed,
# guarded by requires_dir). If the user already maintains their own
# ~/.vibe/hooks.toml, the write is refused (no managed marker), so we never
# clobber it — vibe just goes unreported rather than broken.
#
# Verified against vibe 2.21.0 (`vibe.core.hooks.models.HookConfig`): each
# `[[hooks]]` entry needs `name` + `type` (one of `pre_tool` / `post_tool` /
# `post_agent`) + `command`. Events: pre_tool → working, post_agent → done.
# No `blocked` — vibe's only hook types are pre_tool/post_tool/post_agent
# (no permission/notification event), so a tool awaiting approval reads as
# `working` (pre_tool fires before the approval prompt). If a future vibe
# renames the types/fields, edit vibe-hooks.toml (no code change).
[[external_files]]
path = "~/.vibe/hooks.toml"
source = "vibe-hooks.toml"
requires_dir = "~/.vibe"

# pi (the pi.dev CLI) auto-discovers TypeScript extensions from
# ~/.pi/agent/extensions/*.ts, so we drop a managed file in (only when pi is
# installed, guarded by requires_dir). It subscribes to pi's lifecycle events:
# session_start → idle, agent_start + tool_execution_start → working (a tool call
# to ask_user_question → blocked), agent_end → done. As with the other
# config-dir drops the write is refused (no managed marker) if a file already
# exists at that path, so pi simply goes unreported rather than broken.
#
# EXPERIMENTAL: the event names are verified against pi 0.80.x; if a future pi
# renames them, edit pi-status.ts (no code change). pi has no claude-style
# Stop/permission hook, so `blocked` is inferred only from a structured
# ask_user_question tool call — a turn that ends by asking something in prose
# signals `done`, not `blocked`. As with the other config-dir agents, remote
# (SSH/WSL) pi sessions are provisioned at spawn time (the rewritten payload is
# shipped into the host's ~/.pi/agent/extensions/); the one carve-out is a
# psmux/Windows host, which shows `Hooks: degraded` instead.
[[external_files]]
path = "~/.pi/agent/extensions/thurbox-status.ts"
source = "pi-status.ts"
requires_dir = "~/.pi/agent"

# omp (Oh My Pi) is Pi-compatible and auto-discovers TypeScript extensions from
# ~/.omp/agent/extensions/*.ts, so we drop a managed file in (only when omp is
# installed, guarded by requires_dir). It mirrors pi's status extension but maps
# OMP's structured user-question tool — named `ask` (upstream pi uses
# `ask_user_question`) — to `blocked`; both names are recognized. Events:
# session_start → idle, agent_start + tool_execution_start → working (ask /
# ask_user_question → blocked), agent_end → done. As with the other config-dir
# drops the write is refused (no managed marker) if a file already exists at that
# path, so omp simply goes unreported rather than broken.
#
# EXPERIMENTAL: verified against OMP 17.0.6. As with the other config-dir agents,
# remote (SSH/WSL) omp sessions are provisioned at spawn time (the rewritten
# payload is shipped into the host's ~/.omp/agent/extensions/); the one carve-out
# is a psmux/Windows host, which shows `Hooks: degraded` instead.
[[external_files]]
path = "~/.omp/agent/extensions/thurbox-status.ts"
source = "omp-status.ts"
requires_dir = "~/.omp/agent"

# GitHub Copilot CLI (the `copilot` command) loads hooks from its own dir,
# ~/.copilot/hooks/*.json (each file loaded alphabetically), so we drop a managed
# standalone file in — never touching the user's other hook files. Guarded by
# requires_dir so it's a no-op when copilot isn't installed; the hooks/ subdir is
# created automatically. Events (copilot's own schema, see
# https://docs.github.com/en/copilot/reference/hooks-configuration):
# sessionStart → idle, userPromptSubmitted/preToolUse → working, agentStop →
# done, and notification (matched to `permission_prompt`) → blocked. Both `bash`
# and `powershell` commands are shipped so status works on Windows too. The
# `session signal` JSON output carries none of copilot's decision keys
# (permissionDecision/decision/additionalContext), so it never affects the
# preToolUse/agentStop fail-open flow. If a future copilot changes the schema,
# edit copilot-hooks.json (no code change).
[[external_files]]
path = "~/.copilot/hooks/thurbox-status.json"
source = "copilot-hooks.json"
requires_dir = "~/.copilot"

# antigravity (the `agy` CLI, the Gemini CLI successor) loads global hooks from
# ~/.gemini/config/hooks.json, which we JSON-merge into under a top-level "thurbox"
# key (reversibly; uninstall prunes our key, leaving existing user hooks intact).
# Guarded by requires_dir so it's a no-op when antigravity isn't installed.
#
# Events: PreInvocation fires when a turn begins (maps to `working`),
# PreToolUse on `ask_.*` tools maps to `blocked`, PostToolUse maps back to
# `working`, and Stop fires when the loop terminates (maps to `done`).
# Hook commands must emit a valid JSON object on stdout.
# Identity needs $THURBOX_SESSION to reach the hook process; if agy sanitizes the
# hook env the signal is a fail-open no-op (`|| true`).
[[config_merges]]
path = "~/.gemini/config/hooks.json"
source = "antigravity-hooks.json"
requires_dir = "~/.gemini"

# grok (xAI's Grok Build CLI) loads hooks from ~/.grok/hooks/*.json, each file
# read on its own, so we drop a managed standalone file in rather than touching
# any file the user owns. Guarded by requires_dir so it's a no-op when grok isn't
# installed. Global hooks are ALWAYS trusted and load on first launch, which is
# why this is the global dir and not <project>/.grok/hooks — the project scope
# additionally needs the folder granted trust in grok's own
# ~/.grok/trusted_folders.toml (or a `--trust` launch flag, which would only
# cover sessions thurbox itself launches).
#
# Grok is Claude-Code-compatible, so the mapping mirrors claude.json:
# SessionStart → idle, UserPromptSubmit/PreToolUse/PostToolUse → working,
# Notification (its `message` matched to permission/approval) → blocked,
# Stop → done.
# The blocked command deliberately contains no `$` — a `$VAR` reference in a
# grok hook command must carry an inline `:-default` or grok silently refuses to
# load the whole hook file — so it pipes the extracted message through `grep`
# instead of claude's `case`.
#
# EXPERIMENTAL: the event names are grok's documented set; if a future grok
# renames them, edit grok-hooks.json (no code change).
[[external_files]]
path = "~/.grok/hooks/thurbox-status.json"
source = "grok-hooks.json"
requires_dir = "~/.grok"

# kimi (Kimi Code CLI) reads hooks from a `[[hooks]]` array in ONE shared file,
# ~/.kimi-code/config.toml — there is no drop-in hooks dir, and that file holds
# the user's whole configuration, so a managed external_file would clobber it.
# It is merged instead, exactly like codex/antigravity, with `format = "toml"`
# selecting `agent::toml_merge` (toml_edit, so the user's comments and key order
# survive); uninstall prunes exactly our entries by the `session signal` marker.
#
# Events: SessionStart → idle, UserPromptSubmit/PreToolUse/PostToolUse →
# working, PermissionRequest → blocked, PermissionResult → working, Stop → done.
# Unlike claude/grok the block edge is STRUCTURED (a real
# permission-request event, not a text match on a notification body) and has a
# real clearing event, so it neither false-fires nor latches.
#
# EXPERIMENTAL: kimi accepts exactly four keys per hook entry
# (event/command/matcher/timeout) and refuses to load the whole config file on a
# fifth, so kimi-hooks.toml must never grow one. If a future kimi renames the
# events, edit kimi-hooks.toml (no code change).
[[config_merges]]
path = "~/.kimi-code/config.toml"
source = "kimi-hooks.toml"
requires_dir = "~/.kimi-code"
format = "toml"
